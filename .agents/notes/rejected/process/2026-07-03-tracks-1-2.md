# Agent Note: Tracks 1-2 Deferred Scope
Status: rejected — historical scratch imported from private Incomplete
Origin: Ai-notes/Incomplete/2026-07-03-tracks-1-2.md
Imported: 2026-08-20 from private Incomplete import pass

---
# Incomplete — Tracks 1–2 (2026-07-03)

Done: public credibility docs/config (Track 1) and five lifecycle contract test modules (Track 2). Full suite: 60 passed.

Not done (intentionally deferred):

- **Track 3** (train.py extraction, freeze-after-init for `force_eager`, dependency lockfile) — saved for later per request.
- Ablation ledger metrics were curated from recon notes / private artifact *names*, not re-run campaigns. Numbers may need a maintainer pass before citing externally.
- `wandb.entity: null` and `data.dataset: ~/.cache/...` change public defaults; maintainer must override if their W&B entity or shard path differs from the portable defaults.
- Phase-transition smoke does **not** require density ≤ 0.25 on tiny dims (block-row floor); it only asserts density fell and compact/route flags fire.
- Decode smoke uses `bigram_hash_vocab>0` because `decode_step` does not handle `get_bigram() is None` (possible small inference fix later, not in this pass).
