# Bisect round-4 cell BGpc — GLA DETACHED FROM THE LOOP: retention kept at
# prelude.1 and coda.1, REMOVED from core.1; cap off (post-BG0C0 reference
# frame). Discriminates in-loop bypass vs global gradient stealing against
# BC0 (GLA everywhere, cap off) and BG0C0 (GLA nowhere, cap off).
# Prereg: 2026-08-31-loop-killer-bisect.md round-4 amendment.
defaults:
  - notul_l2
  - _self_

model:
  retention: true
  retention_sections: [prelude, coda]

training:
  spectral_project_cap: 0.0

wandb:
  name: notul-bgpc
