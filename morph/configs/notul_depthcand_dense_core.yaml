# E20 arm "dense-core": ternary QAT everywhere EXCEPT inside the looped core blocks
# (model.core) — the per-pass map stays bf16 while prelude/coda stay ternary. Diagnostic:
# tests whether the ternary snap on the shared core weights is what starves the loop's
# depth-earning (E19: K3−K6 0.0009 nats). Everything else is the E19 loop-entry recipe.
# Prereg: lab/experiments/planned/2026-09-09-arc-e20-loop-depth-candidates.md
defaults:
  - notul_parcae_entry
  - _self_

training:
  ternary_scope: backbone_no_core

wandb:
  name: depthcand-dense-core
