# Write-side ladder arm W1 — "content" slot seed: bag-mean WITHOUT the shared
# E_slot additive term. Prereg: lab/experiments/planned/2026-09-01-write-side-ladder.md.
#
# E2 (lab/experiments/failures/2026-09-01-bound-seed-rank.md) measured that the
# shared E_slot constant collapses every slot seed to ~rank-1 (unit rank 1.13 for
# "bag" with E_slot, vs 3.47 for "bag_noeslot" without it). ONE change from tul_g0c0
# (the 20k h2h TUL winner-recipe arm): `tul.slot_seed: content` — no new parameters,
# no new buffers (morph/model/tul.py `TULSlots.slot_input`).
defaults:
  - tul_g0c0
  - _self_

tul:
  slot_seed: content

wandb:
  name: tul-w1
