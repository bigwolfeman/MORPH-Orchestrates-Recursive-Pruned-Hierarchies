# Lab

Unstructured spikes that are not production and not Agent Notes. Promote into `morph/` with tests, or delete. Decision rationale goes in `.agents/notes/`, not here.

## divergence/ — the TUL core-takeover campaign

`divergence/takeover-campaign.md` is the RUNNING INDEX: every hypothesis tried, its
verdict, the number that decided it, the instruments already built and the traps already
fallen into. Read it before adding an arm — 15 hypotheses have been tried and 11 are
refuted. The probes it names are its neighbours in that directory.
