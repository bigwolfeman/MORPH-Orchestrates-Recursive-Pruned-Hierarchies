# Planned: GL1 — the gist baseline: mask + gradient-carrying tap + SIGReg writes, no loop

Status: planned
Date: 2026-08-29
Config: `morph/configs/tul_gl1.yaml` (build in progress; this file's Predictions are
frozen NOW, before the build lands — only Method's exact commands are filled in as a
dated pre-launch amendment when the builder reports)
Decision record: `.agents/notes/rejected/architecture/2026-08-29-gist-loop.md` (+ both addenda)
Prior: FM2 (`failures/2026-08-29-tul-fm2.md`) closed the training-pressure family on
the additive prefix interface; TG campaign (`failures/2026-08-27-tg-restriction.md`)
measured the mask with a looped, homogeneous write.

## Question

Does the unexplored cell — gisting mask (`tg_restrict`) + boundary-tap write with
FULL gradient + SIGReg on the written slot states + NO loop anywhere — produce the
first slot channel whose CONTENT is load-bearing (shuffle-sensitive), at an
acceptable CE cost vs its own unrestricted twin?

## Hypothesis

TG4b showed the pipe reads values (wrong-plan cost 0.48–0.56 nats) while shuffle
cost ~0 — the writes were homogeneous, not the reads absent. SIGReg at the write
attacks exactly that; the tap (not bag-mean) removes the measured −0.5 power-law
dilution; removing the loop removes both the takeover and the identity-map bias.
I put the headline gate at even odds — the homogeneity fix is proven machinery
applied at an unproven site.

## Predictions (frozen 2026-08-29, before the build completed)

- **L1 stability.** Both arms complete 4500 steps; no sustained detonation
  (`val/ce_tokens` >0.20 above running min for 2+ consecutive evals after 1000).
  New dynamics (gradient flows through slots into earlier spans) but no iterated
  map. Predict HOLDS.
- **L2 THE GATE — shuffle worth.** `worth_shuffle` ≥ 0.05 nats sustained (≥3
  consecutive evals) by 4500 on the restricted arm. Every prefix-interface arm:
  ≤ 0.0006. TG2's zero-worth was 0.03–0.155 but shuffle ~0. Predict ~50%.
- **L3 homogeneity dial.** SIGReg moves the writes: slot-state pairwise cos
  < 0.2 and eff_rank ≥ 64/1024 by 4500. Predict HOLDS (the FM targets moved in
  250 steps under the same regularizer).
- **L4 value sensitivity.** Wrong-plan probe cost ≥ 0.10 nats (TG family:
  0.48–0.56). Predict HOLDS — the mask makes it nearly structural.
- **L5 THE PRICE — restriction cost.** Restricted GL1 vs its unrestricted twin
  at matched steps: ≤ 0.15 nats on `val/ce_tokens` @4250. TG measured 0.17–0.42
  with the worse write; the tap+SIGReg+no-loop bet is that most of that gap was
  write quality. Predict: cost lands in [0.05, 0.20] — genuinely uncertain
  whether it clears the 0.15 line.
- **L6 instrument check.** Under the mask a span's first token cannot attend the
  previous span's tokens, so `first_tok_counterfactual` should flip positive
  near-structurally. If it stays negative, the metric or the mask is broken —
  investigate before interpreting anything else.

## Verdict rule

- SUCCESS = L1 & L2 & L5. → GL2 (the detached refiner loop on top of working
  writes), then GL3 (plan-as-router).
- L2 fires but L5 fails (> 0.15 nats): content-bearing slots exist but reliance
  is overpriced — GL3's router value and the CW/distill hybrids become the
  question; the result is still the program's first load-bearing content.
- L2 silent with L3 held (diverse writes, still shuffle-dead): the tap's content
  is not span-distinctive even when decorrelated — the write REPRESENTATION is
  the next suspect (what to write, not how); the trained-oracle ceiling run
  becomes mandatory before any further arm.
- L1 failing is new information about mask+gradient dynamics; its own finding.

## Method

Two sequential runs (UPS rule), seed 1 each, panel keys (steps 4500, batch 6,
eval_every 250, ckpt_every 500, alpha_cap 3.5, t_beta3=3500, use_kernels=false —
forced by tg_restrict anyway):
1. `tul_gl1` (restricted).
2. The twin: identical except `tul.tg_restrict=false`.
Exact commands: to be appended from the builder's report as a dated pre-launch
amendment. Checkpoint pruning: intermediates deleted after each verdict, finals
kept (the 2026-08-29 disk policy).

## Not verified before build/launch

Whether `tg_restrict` composes with `n_core=0` (builder instructed to STOP on
structural incompatibility); GPU behavior of SIGReg at the write site; whether
the boundary tap at n_core=0 changes the TG eager-only constraint.

## Method amendment (2026-08-29, pre-launch, after the build landed at `769b50c`)

1. **Exact launch:** runner `morph-scratch/tulfm/gl1_run.sh` — both arms
   `--config-name tul_gl1` with the panel keys (steps 4500, batch 6, seed 1,
   alpha_cap 3.5, `training.ademamix_t_beta3=3500` CLI override — the config pins
   4500; 3500 is the panel pin every comparison run used), plus
   `tul.eval_ablations=true` (the GL1 instruments ship behind a default-false
   knob so they don't change eval cost on other arms). Arm 2 adds
   `tul.tg_restrict=false`. wandb names `gl1-s1` / `gl1-ctrl-s1`.
2. **The control is config-matched, NOT parameter-matched:** `tg_restrict`
   replaces the HCA branch, so the unrestricted twin carries +2.037M params
   (+0.98%). Pre-existing for every TG arm and true of the 0.17–0.42 reference
   band too. L5 is scored as written, with this asymmetry named in the writeup.
3. **`slot_seed: boundary` is a seed-level tap** (`E_slot + W_sent·embed(t_last)`),
   not the faithful mid-prelude hidden-state tap the decision note described —
   `tul_tg4b.yaml`'s own comment says so. The slot position still runs the full
   prelude under the mask, so what the coda reads IS a contextualized state. A
   faithful mid-layer tap is a named follow-up arm, not silently included.
4. **The coreless cell was structurally unreachable until this build:**
   `_tul_core` raised at `n_core=0`; the builder added the seed-path analogue of
   `_core_region`'s coreless branch. So no prior arm ever ran this cell — the
   "unexplored" claim is now mechanically confirmed.
5. Predictions above are UNCHANGED (frozen before the build; the builder's
   3-step smoke numbers were reported to me only as "instruments live" and do
   not touch the frozen odds).

---

## Results (filed 2026-08-29; gl1-s1 `qbr69h5r` 27 min @3.44 sps, gl1-ctrl-s1 `hvxl7vky` 26 min; both exit 0)

Status: failure by the letter (L5, L3-rank, L6 failed) — carrying the program's
FIRST measured load-bearing slot content.

Artifacts: `lab/experiments/results/2026-08-29-tul-gl1/eval_history.json` (both arms).

| Prediction | Result | Score |
|---|---|---|
| L1 stability | both completed; gl1 max rise 0.0775, zero consec >0.20 | HELD |
| **L2 GATE: worth_shuffle ≥ 0.05 sustained** | **0.0556 / 0.0696 / 0.0636 at 3750/4000/4250 — three consecutive. Range 0.04–0.07 from step 250.** Control twin: 0.001–0.005. Every prefix arm ever: ≤ 0.0006. | **FIRES (I priced it 50%)** |
| L3 homogeneity | pairwise cos 0.003 (HELD) but eff_rank 41.5 < 64 (FAILED). Oddly the CONTROL reached 62.5 without needing the content. | SPLIT |
| L4 value sensitivity ≥ 0.10 | wrong-value probe 0.33–0.43 (control: 0.005–0.03) | HELD |
| **L5 PRICE ≤ 0.15 nats vs twin** | **0.3654 @3000, 0.3377 @4250** — above the gate AND above my [0.05, 0.20] predicted range; sits inside TG's old 0.17–0.42 band | **FAILED** |
| L6 counterfactual flips positive | −1.3 to −2.6 — an instrument-understanding error on my part: with emit_weight=0 the slot position's readout is untrained, so the metric measures an untrained head, not the mask. Prereg said investigate before interpreting; investigated; the metric is not valid for emit-off arms. | FAILED (mis-specified) |

Speed correction to the build's risk note: NO eager penalty — 3.44 sps
(restricted) vs FM's 3.28–3.30; the removed HCA branch paid for the mask.

## Verdict — the pre-registered branch

"L2 fires but L5 fails: content-bearing slots exist but reliance is overpriced."
Two conclusions, both earned:

1. **The interface question is ANSWERED.** Under the mask, with a
   gradient-carrying write and SIGReg-decorrelated states, the coda reads slot
   CONTENT: shuffling costs 0.06, zeroing 0.15–0.18, a wrong value 0.33–0.43 —
   against ≤ 0.0006 for every additive-prefix arm. The write/read pair works
   when the write carries gradient and the alternative is closed. TG Table 1
   and the gisting configuration are confirmed in OUR model.
2. **The write-quality bet is DEAD.** The tap + SIGReg + no-loop write did not
   close the restriction's CE cost (0.34, inside TG's 0.17–0.42 with a worse
   write). The price of the mask at this budget is intrinsic-ish: ~53 slots
   cannot yet replace what full attention provided. Note: the worth metrics were
   still RISING at 4500 (worth_zero 0.05→0.18, not plateaued) — the price may
   fall with training length; unmeasured.

## Updated hypothesis + next steps (for Wolfe's call)

The bottleneck moved for real this time: from "nothing reads the slots" to "the
slots do not yet carry ENOUGH." Ranked follow-ups:
- **GL2 with the planner as refiner** (the staged plan): richer slot states via
  the detached FM refiner, now writing into an interface that provably reads.
- **Distill the gap** (teacher = unrestricted twin, student = GL1; match hidden
  states): directly converts the 0.34-nat gap into training signal.
- **Longer budget** (cheapest datum): the worth curves were not flat; a 12k-step
  GL1 pair prices the trend.
- **Faithful mid-prelude tap** (the builder's named follow-up): the shipped
  `boundary` seed is `E_slot + W_sent·embed(t_last)` — a seed-level tap; the TG
  paper's actual tap is a mid-layer hidden state, strictly richer.
